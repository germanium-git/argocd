external-dns:
  sources:
    - ingress
    - gateway-httproute
    - crd

  policy: sync

  provider:
    name: cloudflare

  env:
    - name: CF_API_TOKEN
      valueFrom:
        secretKeyRef:
          name: cloudflare-external-dns
          key: apiKey
